[alpaca]
api_key = "replace-in-local-file-or-cloud-secrets-panel"
secret_key = "replace-in-local-file-or-cloud-secrets-panel"
option_feed = "indicative"
stock_feed = "iex"

# Optional durable cloud portfolio database, implemented in a later migration:
# [database]
# url = "postgresql+psycopg://..."

